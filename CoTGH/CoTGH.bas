#Include "graphwrapper.bi"

'graph wrapper for loading maps and tracking unused actor banks
'tilesets create transparency map, use in quadmodel
'stuff for artist

Dim As GraphWrapper graph = GraphWrapper("res/example.tmx")
